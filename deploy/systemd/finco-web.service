[Unit]
Description=FincoGPT Web Application
After=network.target

[Service]
Type=notify
User=finco
Group=finco
WorkingDirectory=/opt/finco1

# Activate the virtual environment
Environment="PATH=/opt/finco1/.venv/bin"
ExecStart=/opt/finco1/.venv/bin/gunicorn main_web:app \
    --workers 2 \
    --bind 127.0.0.1:8000 \
    --timeout 120 \
    --access-logfile /var/log/finco-web/access.log \
    --error-logfile  /var/log/finco-web/error.log \
    --capture-output \
    --graceful-timeout 30

# Restart policy
Restart=on-failure
RestartSec=5

# Environment file (contains secrets)
EnvironmentFile=/opt/finco1/.env

# Security: no new privileges
NoNewPrivileges=true
# Security: don't split environment
# (systemd default; explicit for clarity)

[Install]
WantedBy=multi-user.target