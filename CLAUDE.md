# Finco1 — Claude Instructions

## PR Workflow

After completing any implementation task — once the code is committed and pushed — **automatically open a PR against `main` without waiting for a follow-up prompt**.

Use the designated development branch from the session's system prompt (e.g. `claude/festive-cerf-uaq5hb`) or the feature branch created for the task. Include a structured PR body (summary, files changed, test results, CI/parity status).
