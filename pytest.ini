[pytest]
markers =
    legacy_excel: Legacy Excel calibration diagnostic tests — not production engine acceptance tests
addopts = -q --tb=short --ignore=tests/test_persistence.py --ignore=tests/test_repository.py