# Finco1 — Reproducible Dependency Constraints
# Generated by Phase 26C (2026-05-31)
#
# Strategy: Option B — constraints file with exact pinned versions.
#   requirements.txt and pyproject.toml are kept as human-maintained lower bounds.
#   This file provides exact versions for CI reproducibility.
#
# Install with:
#   pip install -c constraints.txt -e .
#   pip install -c constraints.txt pytest
#
# Upgrade policy:
#   Intentional single-package upgrades only, tested before merge.
#   No automatic broad upgrades in pilot-use branches.
#
# Python 3.12 compatibility note (STAB-2):
#   bcrypt 3.x uses cffi/C extensions; no cp312 wheels exist.
#   bcrypt 4.x is Rust-based with cp312 wheels — use 4.2.0+.

bcrypt==4.2.0
fastapi==0.136.1
httpx==0.28.1
itsdangerous==2.2.0
jinja2==3.1.6
numpy==1.26.4
openpyxl==3.1.5
pandas==2.2.3
pydantic==2.13.2
pytest==9.0.3
python-multipart==0.0.27
uvicorn==0.46.0
