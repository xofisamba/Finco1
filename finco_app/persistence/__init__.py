"""
finco_app.persistence — Persistence layer.

Extraction target (V2-8): storage for projects, scenarios, engine runs,
audit snapshots, and export artefacts. Database-agnostic interface;
initial implementation targets SQLite/PostgreSQL via SQLAlchemy.
"""
